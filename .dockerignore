# =====================================================================
# 根 .dockerignore —— docker-compose.yml 两个服务的 build context 都是项目根，
# 因此必须放在根目录才会被 Docker 读取（apps/*/.dockerignore 不会生效）。
# 目标：把发送给 Docker daemon 的构建上下文压到最小，避免把 node_modules、
# .venv、本地 SQLite、日志、截图、简历样本等无关内容打进上下文。
# =====================================================================

# ---- 依赖与构建产物 ----
**/node_modules/
**/.next/
**/out/
**/dist/
**/build/
**/.turbo/

# ---- Python ----
**/__pycache__/
**/*.py[cod]
**/.venv/
**/venv/
**/.pytest_cache/
**/.mypy_cache/
**/.ruff_cache/
**/*.egg-info/

# ---- 本地运行时数据（绝不进镜像）----
**/app.db
**/app.db-shm
**/app.db-wal
**/*.log
**/logs/

# ---- 环境变量（敏感，仅 .env.sample 可入镜像）----
**/.env
**/.env.local
**/.env.*.local
!**/.env.sample

# ---- Git / IDE / OS ----
.git/
.gitignore
.gitattributes
.vscode/
.idea/
**/.DS_Store
**/Thumbs.db
.claude/
.playwright-mcp/

# ---- 文档与杂项（镜像运行不需要）----
README.md
docs/
LICENSE
assets/
**/*.md

# ---- 根目录本地测试材料（前端 public 下的品牌/编辑器资源必须保留）----
/*.png
/*.jpg
/*.jpeg
/*.gif
/*.webp
/*.docx
/*.pdf
/*.txt
